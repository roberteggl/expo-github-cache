# Changelog

All notable changes to this project will be documented in this file.

## [0.5.0] - 2026-03-22

### 🐛 Bug Fixes

- **deps**: Move TypeScript from `peerDependencies` to `devDependencies` — consumers no longer need to pin TypeScript 5.x or exclude it from `expo install`

### 🚜 Refactor

- Migrate tests and internal types from deprecated `ResolveRemoteBuildCacheProps` to `ResolveBuildCacheProps` and `UploadBuildCacheProps`
- Import `RunOptions` directly from `@expo/config` instead of deriving it from deprecated types

### ⚙️ Miscellaneous Tasks

- Bump `@expo/config` to `^12.0.14` for Expo SDK 57 compatibility

<!-- generated by git-cliff -->
